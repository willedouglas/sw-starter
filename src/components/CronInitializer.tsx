// This is a server component that initializes cron jobs.
// It doesn't render anything, just initializes the service.

import "@/config/initialize-statistics";

export default function CronInitializer() {
  return null;
}
